
```
% dasel -f manifest.yaml -r yaml 'overall.[0]'
name: example1
replaceable_a: oldvalue
keep_this: safe
```

To do a put on a particular location.

```
dasel put -t string -v foo -f manifest.yaml -r yaml 'overall.[0].replaceable_b'
```